# Inference / serving (CUDA wheels often installed separately; see scripts/setup_instance.sh)
vllm>=0.6.0
autoawq>=0.2.0
lm-eval>=0.4.0

# Disaggregated prefill proxy (mirrors vLLM benchmark proxy deps)
quart>=0.19.0
aiohttp>=3.9.0

# Benchmarks + analysis
pandas>=2.0.0
matplotlib>=3.8.0
jupyter>=1.0.0
